# Read the Docs build. Versions are activated in the project's dashboard; this
# file is what every one of them is built with.
#
# https://docs.readthedocs.io/en/stable/config-file/v2.html
version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.12"
  apt_packages:
    # The CPU kernels are compiled from source here, as they are anywhere a
    # wheel does not exist; ``cmake`` and ``ninja`` arrive as build-time
    # wheels through scikit-build-core.
    - build-essential
  jobs:
    post_checkout:
      # setuptools-scm reads the version from the tags, and Read the Docs
      # clones shallow.
      - git fetch --unshallow || true
      - git fetch --tags || true

sphinx:
  configuration: docs/conf.py
  fail_on_warning: false

formats:
  - htmlzip

python:
  install:
    # ``examples`` is what sphinx-gallery imports while it executes the
    # thirteen example scripts. Drop it, and pass ``-D plot_gallery=0`` in
    # ``sphinx.builder`` options, to build the pages without running them.
    - method: pip
      path: .
      extra_requirements:
        - doc
        - examples
